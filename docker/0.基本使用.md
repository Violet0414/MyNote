### docker的基本使用

#### 常用命令

```powershell
docker ps  # 查看当前在运行的容器
docker-compose up -d # 启动容器
docker compose down # 停止容器运行
docker-compose -p xxx up -d # 启动名为xxx的容器
docker-compose -p xxx down	# 停止名为xxx的容器运行
```

