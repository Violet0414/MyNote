### Dify的本地化部署和使用

```powershell
git clone https://github.com/langgenius/dify.git
cd dify/docker
cp .env.example .env
docker-compose up -d
```

​	其实就是和ragflow下载差不多的流程，复制一份配置文件进行配置的修改，然后使用docker启动项目，需要使用科学上网，不然在dify中可能访问不到插件市场。