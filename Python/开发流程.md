#### 项目创建流程

```python
# 1. 创建项目目录
mkdir my-llama-api
cd my-llama-api

# 2. 创建目录结构
mkdir src, src\api, src\core, src\services, src\models, src\utils, tests, static, templates

# 3. 创建 __init__.py 文件
ni "src\__init__.py"
ni "src\api\__init__.py"
ni "src\core\__init__.py"
ni "src\services\__init__.py" 
ni "src\models\__init__.py"
ni "src\utils\__init__.py"
ni "tests\__init__.py"

# 4. 创建 requirements.txt
@"
fastapi==0.104.1
uvicorn==0.24.0
llama-index==0.9.48
openai==1.3.8
python-multipart==0.0.6
python-dotenv==1.0.0
pydantic==2.5.0
"@ | Out-File -FilePath "requirements.txt" -Encoding utf8

# 5. 创建 .env 文件
@"
OPENAI_API_KEY=your_openai_api_key_here
"@ | Out-File -FilePath ".env" -Encoding utf8

Write-Host "项目结构创建完成！" -ForegroundColor Green

my-llama-api/
├── 📁 src/                    # 主要代码都在这里
│   ├── 🐍 main.py            # 应用入口（类似前端的index.js）
│   ├── 📁 api/               # API路由（类似routes/）
│   │   └── 🐍 routes.py      # 定义所有API端点
│   ├── 📁 core/              # 核心配置（类似config/）
│   │   └── 🐍 config.py      # 环境变量和配置
│   ├── 📁 services/          # 业务逻辑（类似services/）
│   │   └── 🐍 llama_service.py # LLM处理逻辑
│   ├── 📁 models/            # 数据模型（类似types/）
│   │   └── 🐍 schemas.py     # 请求/响应数据格式
│   └── 📁 utils/             # 工具函数
├── 📁 tests/                 # 测试文件
├── .env                      # 环境变量
└── requirements.txt          # 依赖列表
```



#### 常用命令

```python
# 检查当前Python路径
which python

# 查看已安装的包
pip list

# 从requirements.txt安装依赖
pip install -r requirements.txt

# 创建虚拟环境
python -m venv venv

# 激活虚拟环境
# Windows
venv\Scripts\activate

# 安装项目依赖
pip install requests pandas numpy

# 生成requirements.txt
pip freeze > requirements.txt

# 停用虚拟环境
deactivate

#清除缓存
pip cache purge
```



#### 基础项目结构对比

```
Python项目 (Django示例)             前端项目 (React示例)
my_backend/                         my_frontend/
├── venv/                           ├── node_modules/
├── manage.py                       ├── package.json
├── requirements.txt                ├── package-lock.json
├── my_project/                     ├── public/
│   ├── __init__.py                 │   ├── index.html
│   ├── settings.py                 │   └── favicon.ico
│   ├── urls.py                     └── src/
│   └── wsgi.py                         ├── index.js
└── my_app/                             ├── App.js
    ├── __init__.py                     ├── components/
    ├── models.py                       └── styles/
    ├── views.py
    ├── urls.py
    └── templates/
```

---



#### 核心文件夹/文件功能详解

#### 1. 依赖管理（类比 `node_modules` 和 `package.json`）

| Python                                | 前端                | 作用                       |
| :------------------------------------ | :------------------ | :------------------------- |
| **`venv/`** 或 **`.venv/`**           | **`node_modules/`** | 存放项目所有依赖包的文件夹 |
| **`requirements.txt`**                | **`package.json`**  | 记录项目依赖列表的文件     |
| **`pip install -r requirements.txt`** | **`npm install`**   | 安装项目所有依赖的命令     |

**示例 `requirements.txt`：**

```
Django==4.2.0
requests>=2.25.0
pandas==1.5.0
```

#### 2. 项目配置文件（类比 `package.json` 的 scripts）

| Python                                 | 前端                            | 作用               |
| :------------------------------------- | :------------------------------ | :----------------- |
| **`manage.py`** (Django)               | **`package.json` 中的 scripts** | 项目管理和启动脚本 |
| **`setup.py`** 或 **`pyproject.toml`** | **`package.json`**              | 项目配置和元数据   |

**`manage.py` 常用命令（类比 npm scripts）：**

```python
# 类似 npm start
python manage.py runserver

# 类似 npm run build
python manage.py collectstatic

# 类似 npm test
python manage.py test
```

#### 3. 源代码组织

#### Django 项目结构（类似 Create React App）

```
my_backend/
├── my_project/          # 类似 src/ 根目录
│   ├── settings.py      # 类似 vite.config.js 或 webpack 配置
│   ├── urls.py          # 类似 React Router 路由配置
│   └── wsgi.py          # 服务器部署配置
└── my_app/              # 类似 src/components/ 功能模块
    ├── models.py        # 数据模型（类似定义接口数据类型）
    ├── views.py         # 视图函数（类似 React 组件）
    ├── urls.py          # 子路由（类似嵌套路由）
    └── templates/       # HTML模板（类似 JSX）
```

#### Flask 项目结构（类似 Vite 或简单项目）

```
my_flask_app/
├── app.py               # 类似 main.js 或 App.jsx
├── templates/           # 类似 public/ 或 HTML 文件
├── static/              # 类似 src/assets/ 静态资源
│   ├── css/
│   ├── js/
│   └── images/
└── requirements.txt     # 类似 package.json
```

---



#### 重点文件夹详细说明

#### `venv/` 文件夹（你的沙箱环境）

- **作用**：就像前端的 `node_modules`，包含项目所有Python包
- **要不要提交Git**：❌ **不要**（应该在 `.gitignore` 中添加 `venv/`）
- **为什么需要**：避免不同项目依赖冲突

#### `__init__.py` 文件（标记文件夹为Python包）

- **作用**：告诉Python"这个文件夹是一个包"，类似前端的 `index.js` 导出文件
- **位置**：在每个Python包目录下都需要
- **内容**：可以是空文件，或包含初始化代码

#### `templates/` 文件夹（存放HTML模板）

- **类比**：前端的 `src/components/` 或 `public/`
- **文件类型**：`.html` 文件，可以使用模板语法（类似JSX）

#### `static/` 文件夹（静态资源）

- **类比**：前端的 `public/` 或 `src/assets/`
- **存放内容**：CSS、JavaScript、图片等静态文件

#### 💡 给前端开发者的实用建议

1. **虚拟环境 = 项目独立的 node_modules**
   - 每个项目都有自己的 `venv`
   - 激活环境 = 确保使用正确的依赖版本
2. **requirements.txt = package.json 的 dependencies**
   - 记录项目依赖
   - 其他开发者通过它安装相同依赖
3. **Python包 = 前端模块**
   - 一个Python包就像前端的一个功能模块
   - 通过 `import` 引入，类似前端的 `import`
4. **Django应用 = 前端的功能组件**
   - 每个应用负责特定功能
   - 可以复用和组合

---



#### 实际项目示例

假设你要开发一个博客系统：

```
blog_project/                    blog_frontend/
├── venv/                        ├── node_modules/
├── requirements.txt             ├── package.json
├── manage.py                    └── src/
├── blog/                           ├── components/
│   ├── models.py      ← 数据层 →      │   ├── Post.js
│   ├── views.py       ← 逻辑层 →      │   └── Comment.js
│   ├── urls.py        ← 路由层 →      ├── App.js
│   └── templates/     ← 视图层 →      └── index.js
│       └── post_list.html
└── project/
    ├── settings.py    ← 配置文件 →     ├── vite.config.js
    └── urls.py        ← 根路由   →     └── 路由配置
```















